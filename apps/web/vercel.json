{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @nextup/types build && pnpm --filter @nextup/web build",
  "outputDirectory": ".next",
  "installCommand": "npm install -g pnpm"
}
